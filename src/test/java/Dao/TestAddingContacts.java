package Dao;
/*
import models.MappedContact;
import org.junit.Before;
import org.junit.Test;
import repository.ContactBase;
import services.ContactServiceImpl;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;


public class TestAddingContacts {
    ContactServiceImpl cs;
    ContactBase cb;
    MappedContact contact;

    @Before
    public void setUp() {
        cb = new ContactBase(5);
        cs = new ContactServiceImpl(cb);
        contact = new MappedContact(1, "Maxim", "Rudenko", "202020");
        cs.addContact(contact);
    }

    @Test
    public void testAddingContacts() {
        cs.addContact(contact);
        System.out.println("method");
        assertTrue(cb.getBase().contains(contact));
        assertFalse(cb.getBase().contains(new MappedContact()));

    }
}*/